# 작업일지 — 2026-03-10 (화)

## 요약

Teams 챗봇 기반 회의실 예약 시스템 초기 구현 완료 및 실제 사이트(mile.im) 스크래핑 검증

## 작업 내역

### 1. 프로젝트 초기 구현 (`ed4bdfa`)

- Node.js + TypeScript 프로젝트 세팅
- Bot Framework SDK v4 기반 Teams 챗봇 구현 (BookingBot, CardBuilder)
- Playwright 기반 스크래퍼 모듈 구현 (SiteAuthenticator, RoomScraper, BookingExecutor, BrowserPool)
- SQLite DB 스키마 및 Repository 계층 (rooms, bookings 테이블)
- p-queue 기반 작업 큐 (동시 1, 대기 5)
- Adaptive Card 검색/결과/도움말 카드 작성

### 2. 사이트 분석 및 셀렉터 업데이트 (`70fae90`)

- `analyze-site.js`, `analyze-booking-flow.js` 스크립트로 mile.im 실제 DOM 분석
- 로그인 필드: `input[placeholder='이메일 주소']` (type="default", React 커스텀)
- 3단계 인증 플로우 발견: 로그인 → 워크스페이스 선택(/workspace/list) → 예약현황
- SiteAuthenticator에 워크스페이스 선택 단계 추가
- selectors.json 전면 교체 (실제 CSS 셀렉터)
- DB에 building 컬럼 추가 (회의실 이름 중복 → name+building+floor로 식별)
- BookingExecutor: 슬라이드 패널 예약 폼 자동화 구현

### 3. 예약 가능 층 확인 및 반영 (`303c86b`)

- 예약 가능 층: **2층, 7층, 8층만** (본관, 씨지인사이드 7층 입주사)
- 도움말 카드 텍스트 수정

### 4. RoomScraper 전면 리팩토링 (`81bc1d4`)

- **필터 기반 조회 방식으로 전환**
  - 회의실 위치 필터(본관) + 층 필터(2/7/8) 드롭다운 선택
  - 기존: 전체 타임그리드 파싱 → 변경: 필터 적용 후 타임그리드 파싱
- 가로 스크롤(`div.scrollLeft += 400`)로 빠짐없이 수집
- **예약 블록 시간 파싱** 구현 (`p.typo.caption02.bold`, "HH:mm ~HH:mm" 형식)
  - 요청 시간대와 겹치는 예약이 있으면 unavailable 판정
- 실제 사이트 테스트로 **10개 회의실** 확인:
  - 2층 본관: 회의실 ①②③
  - 7층 본관: 회의실 ①②③④
  - 8층 본관: 회의실 ①②③

### 5. 기타 (`180158b`)

- `.claude/` 디렉토리 gitignore 추가

## 커밋 로그

| 커밋 | 설명 |
|------|------|
| `c482e4d` | Initial commit |
| `ed4bdfa` | feat: Add Teams meeting room booking chatbot |
| `70fae90` | feat: Update all scrapers with real mile.im site selectors |
| `303c86b` | fix: Update help text to reflect actual bookable floors (2, 7, 8) |
| `81bc1d4` | feat: Rewrite RoomScraper with filter-based room discovery |
| `180158b` | chore: Add .claude/ to gitignore |

## 발견 사항

- mile.im은 React SPA, input type="default" 사용 (표준 아님)
- 필터 input은 editable 아님 → 클릭 → 드롭다운 선택 방식
- 필터 적용 직후 타임그리드에 이전 데이터가 잠시 섞임 → 코드에서 floor 검증 필수
- 드롭다운 옵션 CSS: `div.css-y4bpjy` (건물), 층은 텍스트 매칭

## 남은 작업

- [ ] Azure Bot Service (F0) 등록 및 채널 연결
- [ ] Azure Container Apps 배포
- [ ] 실제 예약 실행(BookingExecutor) E2E 테스트
- [ ] 예약 취소 플로우 E2E 테스트
- [ ] 일일 reconciliation 배치 (사이트 ↔ 로컬 DB 동기화)
- [ ] 단위 테스트 (HTML 스냅샷 목 기반)
