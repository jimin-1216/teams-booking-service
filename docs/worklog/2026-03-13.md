# 작업일지 — 2026-03-13 (목)

## 요약

코드 품질 개선 + Azure Bot Service 등록 + 배포 파이프라인 구성

## 작업 내역

### 1. 코드 품질 개선 (완료)

- **DateNavigator 공통 유틸 추출**: `BookingExecutor.navigateToDate`와 `RoomScraper.setDate`의 55줄 중복 코드를 `src/scraper/DateNavigator.ts`로 통합
- **매직넘버 상수화**: 좌표 기반 하드코딩(`x > 830`, `x < 250` 등)을 `FORM_PANEL_X_MIN`, `ROOM_INPUT_INDEX`, `SIDEBAR_X_MAX` 상수로 변경
- **bulkUpsert 버그 수정**: `RoomRepository.bulkUpsert`에서 `room.building` 인자 누락 (6개 플레이스홀더에 5개 값) 수정
- **cancelBooking 셀렉터 개선**: `querySelectorAll('*')` → 타겟 셀렉터(`a, button, [role="menuitem"], span`)로 변경

### 2. Azure Bot Service 등록 (완료)

- Azure Bot 리소스 생성 완료 (`cg-teams-booking`, SingleTenant, S1)
- Teams Developer Portal에서 Bot management → 클라이언트 비밀 생성
- `.env` 및 `config.ts`에 SingleTenant 설정 반영 (AppId, AppPassword, TenantId)
- `index.ts`에서 `MicrosoftAppTenantId` 설정 추가

| 항목 | 값 |
|---|---|
| 리소스 그룹 | cg-teams |
| 봇 이름 | cg-teams-booking |
| 앱 유형 | SingleTenant |
| 구독 | Azure for Students |

### 3. 배포 파이프라인 구성 (완료)

- `.dockerignore` 생성
- GitHub Actions 워크플로우 작성 (`.github/workflows/deploy.yml`)
  - main push 시 자동 빌드 + Azure Container Apps 배포
  - 환경변수는 Container Apps Secrets로 관리

## 남은 작업 (사용자 수동)

### Azure 포털 작업

1. **서비스 주체 생성** (Cloud Shell에서 실행)
   ```bash
   az ad sp create-for-rbac \
     --name "github-deploy" \
     --role contributor \
     --scopes /subscriptions/4f123e29-0df3-4640-89b7-c0cf74cc7bcf/resourceGroups/cg-teams \
     --sdk-auth
   ```

2. **GitHub Secrets 등록** (레포 → Settings → Secrets → Actions)
   - `AZURE_CREDENTIALS`: 위 명령어 출력 JSON 전체
   - `MICROSOFT_APP_ID`: bd0b1399-0f7f-418d-8d08-cb5f13243413
   - `MICROSOFT_APP_PASSWORD`: (생성한 클라이언트 비밀)
   - `MICROSOFT_APP_TENANT_ID`: a205327f-7bc3-4e59-a5df-c3c537ab3247
   - `MILE_USERNAME`: (예약 사이트 계정)
   - `MILE_PASSWORD`: (예약 사이트 비밀번호)

### 배포 후 작업

3. **Azure Bot 엔드포인트 설정**
   - 배포 완료 후 Container App URL 확인
   - Azure Bot → 구성 → 엔드포인트: `https://<container-app-url>/api/messages`

4. **Teams 채널 추가**
   - Azure Bot → 채널 → Microsoft Teams 추가

5. **Teams 앱 패키지 배포**
   - Developer Portal에서 앱 매니페스트 작성 + 조직 내 배포
